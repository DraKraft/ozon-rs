import { Button, HorizontalBox, VerticalBox} from "std-widgets.slint";

export component MainApp inherits HorizontalBox {
    VerticalBox {
        Text {
            text: "Stuf ...";
        }
    }
}
